# internal / scratch
memory/
_remote_snapshot/
.claude/
CLAUDE.md

# research-idea notes are Jord's personal doc
Persona __ Network Entanglement Research Ideas.md

# large PDF extractions, redundant with quoted content in code
_sonnet45_card*.txt

# scratch files from investigation agents
list_models.py
or_survey_raw*.json
or_verify*.json
survey_logprobs*.py
verify*.py

# local logs / stdout captures (keep sweep.log inside results/ — they're small)
*.stdout

# python artifacts
__pycache__/
*.pyc

# session reset 2026-05-27: stale historical content kept locally, not tracked
_old_remove/

# auto-generated analyzer summary files — regenerable; only commit if pinned
results/*_summary.md
